# AHB Slave Single Read Transaction

## Description

<img width="1920" height="1080" alt="Screenshot from 2026-07-06 08-57-05" src="https://github.com/user-attachments/assets/b2259d99-4331-46e4-b7d1-4e00755e2b64" />


This waveform illustrates the operation of the **AHB Slave** during a **single read transaction**. The slave is selected by asserting `HSEL`, while the master initiates the transfer by driving the target address and control signals. Since `HWRITE` is deasserted, the transaction is identified as a read operation. The transfer type is `NONSEQ`, indicating the start of a new transfer, and `HSIZE` is configured for a 32-bit word transfer with `HBURST` set to `SINGLE`.

After decoding the address (`0x10`), the slave accesses the corresponding memory location and places the stored data (`0x12345678`) onto the `HRDATA` bus. Throughout the transaction, `HREADY` remains asserted, indicating that the slave is ready to complete the transfer without inserting any wait states. The response signal `HRESP` remains low (`OKAY`), confirming successful completion of the read operation.

## Signal Summary

| Signal | Value | Description |
|---------|-------|-------------|
| HSEL | 1 | Slave selected |
| HADDR | 32'h00000010 | Target read address |
| HWRITE | 0 | Read operation |
| HTRANS | NONSEQ (2'b10) | Start of a new transfer |
| HSIZE | 3'b010 | 32-bit word transfer |
| HBURST | SINGLE (3'b000) | Single transfer |
| HRDATA | 32'h12345678 | Data returned by the slave |
| HREADY | 1 | Slave ready; no wait states |
| HRESP | 0 (OKAY) | Successful transfer |

## Transaction Flow

1. The master selects the slave by asserting `HSEL`.
2. The address `0x10` is driven on the `HADDR` bus.
3. `HWRITE = 0` indicates a read transaction.
4. `HTRANS = NONSEQ` marks the beginning of the transfer.
5. The slave decodes the address and accesses the requested memory location.
6. The stored data `0x12345678` is placed on the `HRDATA` bus.
7. Since `HREADY = 1`, the transfer completes without any wait states.
8. `HRESP = OKAY` confirms successful completion of the transaction.



# Integrated AHB Master-Slave Burst Verification

<img width="1920" height="1080" alt="Screenshot from 2026-07-07 02-45-52 (1)" src="https://github.com/user-attachments/assets/e16c2c54-2022-4459-8efc-0ac999e47e2b" />


## Description

This waveform demonstrates the functional verification of burst transfer communication between the AHB Master and AHB Slave. The simulation verifies both **INCR4** and **WRAP4** burst transactions for read and write operations. During each burst, the master generates the appropriate address and control signals while the slave responds with read data, ready, and response signals according to the AMBA AHB protocol.

Initially, the master performs an **INCR4 burst write** by setting `HBURST = INCR4`. The burst begins with `HTRANS = NONSEQ`, followed by `HTRANS = SEQ` for the remaining transfers. The address increments by four bytes after each transfer while the corresponding write data is driven on the `HWDATA` bus.

The simulation then verifies a **WRAP4 burst write**. In this transaction, the addresses wrap around within the burst boundary instead of continuously incrementing. This confirms correct implementation of the AHB wrap burst addressing mechanism.

Following the write operations, the slave returns burst read data on the `HRDATA` bus. The returned data matches the values stored during the previous write transactions, confirming correct memory operation and burst data transfer.

Throughout the simulation, `HREADYOUT` remains asserted, indicating that the slave is always ready to complete the transfers without inserting wait states. The response signal `HRESP` remains low (`OKAY`), confirming successful completion of every burst transaction.

## Transactions Verified

| Transaction | Burst Type | Operation |
|-------------|------------|-----------|
| Burst Write | INCR4 | Write |
| Burst Read | INCR4 | Read |
| Burst Write | WRAP4 | Write |
| Burst Read | WRAP4 | Read |

## Signal Summary

| Signal | Description |
|---------|-------------|
| HADDR | Burst address sequence generated by the master |
| HTRANS | NONSEQ for first beat, SEQ for remaining beats |
| HWRITE | Selects read or write operation |
| HBURST | INCR4 and WRAP4 burst types |
| HSIZE | 32-bit word transfer |
| HWDATA | Write data generated by the master |
| HRDATA | Read data returned by the slave |
| HREADYOUT | Slave ready signal |
| HRESP | Transfer response (OKAY) |

## Features Verified

- INCR4 burst write operation
- WRAP4 burst write operation
- INCR4 burst read operation
- WRAP4 burst read operation
- Sequential address incrementing
- Wrap-around address generation
- Correct NONSEQ and SEQ transfer sequencing
- Burst write data transfer
- Burst read data verification
- No wait-state insertion (`HREADYOUT = 1`)
- Successful transfer completion (`HRESP = OKAY`)

## Observation

The waveform confirms successful communication between the AHB Master and AHB Slave during burst transfers. Both INCR4 and WRAP4 burst modes generate the expected address sequences, while the slave correctly returns the corresponding read data. The continuous assertion of `HREADYOUT` and `HRESP = OKAY` indicates that all burst transactions complete successfully without protocol violations or wait-state insertion.
